export interface BespokeSyncOption {
    key?: string;
}
export interface BespokeSyncState {
    reference: number;
    index: number;
    fragmentIndex: number;
}
export interface BespokeWithSyncKey {
    syncKey: string;
    [key: string]: any;
}
export declare const assertBespokeWithSyncKey: <T extends Record<string, any> = Record<string, any>>(deck: T) => deck is T & BespokeWithSyncKey;
declare const bespokeSync: (opts?: BespokeSyncOption) => (deck: any) => void;
export default bespokeSync;
